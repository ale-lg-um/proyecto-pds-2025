package serviciosDominio;

public class PoliticaTarjetas {
	
	// Valida bloqueo, límite y prerrequisitos
	validarCreacion(tablero, listaDestino);
	
	// Valida bloqueo y límite
	validarMovimiento(tablero, tarjeta, listaDestino);
	
	// Valida que exista lista especial de completadas
	validarCompletar(tablero, tarjeta);
}
